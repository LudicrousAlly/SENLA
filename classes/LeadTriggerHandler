public class LeadTriggerHandler{
    
    public static void setCountryFromPhone(List<sObject> objects){
        QueueableCountryService queueableCountryService = new QueueableCountryService(objects);
        System.enqueueJob(queueableCountryService);
    }
}
